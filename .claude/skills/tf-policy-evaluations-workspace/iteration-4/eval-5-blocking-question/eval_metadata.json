{
  "eval_id": 5,
  "eval_name": "blocking-question",
  "prompt": "Did any policies block run-iURWDL3wVxzefsjo from applying? Tell me which (if any) were mandatory, soft-mandatory overridable, or errored.",
  "assertions": [
    "The response correctly states that the run was NOT blocked by policies — it applied successfully",
    "The mandatory-failed count is identified as zero",
    "The response distinguishes between 'advisory failed' (warns only) and 'mandatory failed' (blocks) and/or 'soft-mandatory overridable' (human can override)",
    "No policy is incorrectly labelled as 'mandatory' or 'errored' — the actual data shows only advisory failures",
    "The response does not false-positive about mandatory/errored/overridable statuses that aren't present in the data",
    "Run lifecycle is referenced (the run was applied) to support the 'did not block' conclusion"
  ]
}
